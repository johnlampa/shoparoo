[phases.setup]
nixPkgs = ['php82Extensions.pgsql', 'nodejs_20']

[phases.build]
cmds = [
	'composer install --optimize-autoloader --no-scripts --no-interaction',
	'npm ci',
	'npm run build'
]

[start]
cmd = 'php artisan storage:link --force && php artisan serve --host=0.0.0.0 --port=$PORT'